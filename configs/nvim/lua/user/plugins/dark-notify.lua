local dn = require('dark_notify')

dn.run({
schemes = {
  dark = {
    colorscheme = "tokyonight",
      background = "dark"
    },
    light = {
colorscheme = "dayfox",
      background = "light"
    }
  }
})
