# General
alias please='sudo'
alias v='fd --type f --hidden --exclude .git | fzf-tmux -p --reverse | xargs nvim'
alias vim='nvim'
alias r='ranger'
alias ranger='ranger --choosedir=$HOME/.rangerdir; LASTDIR=`cat $HOME/.rangerdir`; cd "$LASTDIR"'
alias lg='lazygit'
alias ldo='lazydocker'
alias lse='exa -lhi'
alias lvim="~/.local/bin/lvim"
alias ll="exa -lhi"
# Vim
alias uv='nvim ~/personal/dotfiles'
alias nvd='neovide'

# Command Shortcuts
alias tt='tmux attach -t'
alias ttt='tmux new-session -s'
tls() {
    target=`tmux ls -F "#{session_name}" | fzf-tmux -p --reverse`
    tmux attach -t $target
}
alias tmx='tmuxinator'
alias tkill='tmux kill-session -t'
tkill-all() {
    for i in `tmux ls -F "#{session_name}"`; do tmux kill-session -t $i ; done;
}

# Shortcuts
alias slive='bundle exec jekyll serve'
alias ccd='cd ;exa -i'
alias dev='cd ~/Projects'
alias demos="cd  ~/Desktop"

# Obsidian Vaults

alias vault_thinking='cd ~/Library/Mobile\ Documents/iCloud~md~obsidian/Documents/Thinking'
alias vault_work='cd ~/Library/Mobile\ Documents/iCloud~md~obsidian/Documents/Work'
alias vault_personal='cd ~/Library/Mobile\ Documents/iCloud~md~obsidian/Documents/Personal'
alias vault_capture='cd ~/Library/Mobile\ Documents/iCloud~md~obsidian/Documents/Capture'
alias vault_writing='cd ~/Library/Mobile\ Documents/iCloud~md~obsidian/Documents/Writing'

# dev shortcuts / personal
alias dotfiles='cd ~/personal/dotfiles'
alias android='cd ~/code/android'
alias notebooks='cd ~/code/notebooks'
alias see-git-config='vim ~/.gitconfig'

# Rust related
nrp() {
    cargo new $1; cd $1; nvim
}

# Work
alias activate-doc='source /Users/yigit.ozgumus/mkdocs/bin/activate'
alias demoOn='sh /Users/$(whoami)/.local/bin/adb-demo.sh on'
alias demoOff='sh /Users/$(whoami)/.local/bin/adb-demo.sh off'

# region Android aliases
alias screen="scrcpy --stay-awake --encoder OMX.google.h264.encoder -m 1024 --window-title 'Android' --window-height 800"
alias startApp='adb shell am start -n trendyol.com.stage/com.trendyol.common.splash.impl.ui.SplashActivity'
# endregion

# region Gradle aliases
alias gw="./gradlew $@"
# endregion

# Python
select-env() {
    target=`conda env list | awk '/^[a-z]/{print $1}' | fzf-tmux -p --reverse`
    conda activate $target
}
